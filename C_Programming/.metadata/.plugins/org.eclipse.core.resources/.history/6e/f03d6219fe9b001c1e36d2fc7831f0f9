#include <stdio.h>
#include <stdlib.h>

int main()
{
	int num, sum=0;
	printf("Enter a number: ");
	fflush(stdout);
	scanf("%d", &num);
	sum += num;
	while(num)
	{
		printf("Enter a number: ");
		fflush(stdout);
		scanf("%d", &num);
		sum += num;
	}
	printf("sum = %d",sum);

	return 0;
}
